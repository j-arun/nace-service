
spring:
  datasource:
    url:    jdbc:mysql://localhost:23306/test?createDatabaseIfNotExist=true
    username:   root
    password:   root
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: nace-service
  main:
    allow-bean-definition-overriding: true